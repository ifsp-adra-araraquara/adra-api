# adra-api

Para conectar no banco exporte a senha do supabase assim:

```bash
export SUPABASE_PASSWORD=
```

Para trocar o ambiente ativo (prod ou stage) defina a propriedade:

```bash
spring.profiles.active={prod | stage}
```
