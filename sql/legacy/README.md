# Histórico

Scripts aplicados à mão no Supabase antes do Flyway, e o dump consolidado que
alimentava o `make reset`. Nada aqui é executado por comando nenhum — vários
sequer rodam em banco limpo.

A fonte de verdade do schema é `src/main/resources/db/migration/`.
